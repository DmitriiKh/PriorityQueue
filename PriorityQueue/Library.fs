namespace PriorityQueue

module MaxQueue =
    type Element<'T> = { Priority:uint; Value:'T }

    let insert elements =
        elements

    let extract queue =
        { Priority=10u; Value=10u }, [ { Priority=4u; Value=4u } ]
